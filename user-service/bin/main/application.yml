server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/user-db # DB 주소
    username: root # DB 계정
    password: password # DB 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
